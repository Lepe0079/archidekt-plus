# Backlog

Not urgent, not blocking — parked here for later.

- Add an `icons` entry to `manifest.json` (needed for a proper AMO listing and
  the toolbar/about:addons page; currently none is set).
- Add a `LICENSE` file.
- Add a `package.json` pinning `web-ext` as a devDependency with `lint`/`run`/
  `build` npm scripts, instead of relying on ad-hoc `npx web-ext`.
